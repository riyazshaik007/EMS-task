apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ems-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ema-app
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
